"""Distributions, sampling and sensitivity analysis, as Kompartment does them.

Ports of the application's own statistics (``src/domain/pdf.js``,
``sample.js``, ``correlate.js``, ``gsa.js``, ``salib.js`` and the rest), so a
probabilistic run in Python draws the same samples and reports the same
measures as the application.
"""
